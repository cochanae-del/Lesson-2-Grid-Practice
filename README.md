## Grid Practice
Our blog webpage could use a little organization! Right now, all of our posts show up linearly on the page following the navigation, but let's use CSS grid so our users can see a grid of posts with the navigation along the right side of the page.

Take a look at [grid-practice-screenshot.png](grid-practice-screenshot.png) to see what kind of layout we're going for.

### Hints
Check out the `hints` folder if you need a hint!